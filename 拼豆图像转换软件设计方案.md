# 拼豆图像转换软件设计方案

## 一、项目概述

### 1.1 项目背景
基于对现有拼豆生成网站（如 pixel-beads.com 和 pindou.online）的调研，设计一个功能更全面、体验更优的拼豆图像转换软件，帮助用户轻松将图片转换为拼豆图纸。

### 1.2 核心功能
- 图像上传和自动分析
- 智能配色方案生成
- 实时预览和编辑
- 多格式导出支持
- 图案库管理

## 二、功能设计

### 2.1 图像处理模块

#### 2.1.1 图像上传
- 支持格式：JPG、PNG、WEBP、GIF
- 文件大小限制：最大 10MB
- 拖拽上传支持
- 多图批量上传

#### 2.1.2 图像预处理
- 自动裁剪和居中
- 尺寸标准化（最大支持 100x100 像素网格）
- 亮度/对比度调整
- 边缘检测优化

#### 2.1.3 颜色量化
- 使用 Mard 标准色卡（221色基础版/291色完整版）
- K-means 聚类算法优化配色
- 可手动调整颜色映射
- 色差最小化算法

### 2.2 图案生成引擎

#### 2.2.1 网格映射
- 可选网格大小（10x10 到 50x50）
- 网格对齐优化
- 智能填充算法
- 拼豆间隙模拟

#### 2.2.2 图案渲染
- 实时预览
- 立体效果渲染
- 拼豆重叠效果
- 光影效果模拟

#### 2.2.3 编辑功能
- 单个拼豆修改
- 区域选择和编辑
- 撤销/重做功能
- 图案模板库

### 2.3 配色系统

#### 2.3.1 Mard 色卡集成
- 221色基础版（A-M系列）
- 291色完整版（含P/Q/R/T/Y/ZG系列）
- 颜色筛选和搜索
- 自定义色板创建

#### 2.3.2 智能配色
- 自动推荐配色方案
- 风格模板（复古、现代、卡通等）
- 季节主题配色
- 自定义配色权重

### 2.4 导出功能

#### 2.4.1 图片导出
- PNG/JPG 导出
- 透明背景支持
- 多分辨率导出
- 水印添加

#### 2.4.2 打印导出
- PDF 格式生成
- 网格线叠加
- 拼豆编号标注
- 分页打印优化

#### 2.4.3 数字导出
- SVG 矢量格式
- 拼豆列表数据
- JSON 配置文件
- 3D 模型文件（可选）

## 三、技术架构

### 3.1 前端架构
- **框架**：React 18 + TypeScript
- **状态管理**：Redux Toolkit
- **UI 组件库**：Ant Design
- **画布渲染**：Canvas API / Konva.js
- **文件处理**：File API + Web Workers

### 3.2 后端架构（可选）
- **API 服务**：Node.js + Express
- **图像处理**：Sharp + ImageMagick
- **颜色算法**：Python（OpenCV）API
- **数据库**：MongoDB/PostgreSQL

### 3.3 算法核心

#### 3.3.1 颜色量化算法
```python
def color_quantization(image, n_colors=10):
    # 使用 K-means 聚类
    pixels = image.reshape(-1, 3)
    kmeans = KMeans(n_clusters=n_colors, random_state=42)
    kmeans.fit(pixels)
    
    # 找到最接近的 Mard 颜色
    mard_colors = get_mard_colors()
    quantized = []
    for center in kmeans.cluster_centers_:
        closest = find_closest_mard_color(center, mard_colors)
        quantized.append(closest)
    
    return quantized
```

#### 3.3.2 网格映射算法
```javascript
function mapToGrid(image, gridSize) {
    const cellWidth = image.width / gridSize;
    const cellHeight = image.height / gridSize;
    const beads = [];
    
    for (let y = 0; y < gridSize; y++) {
        for (let x = 0; x < gridSize; x++) {
            const color = getAverageColor(
                image,
                x * cellWidth, y * cellHeight,
                cellWidth, cellHeight
            );
            beads.push({ x, y, color });
        }
    }
    
    return beads;
}
```

## 四、用户体验设计

### 4.1 界面布局
- **左侧工具栏**：颜色板、工具选择
- **中央画布**：图案显示和编辑
- **右侧属性面板**：设置和选项
- **底部状态栏**：进度、提示信息

### 4.2 操作流程
1. 上传图片（拖拽/点击）
2. 自动生成预览
3. 调整网格大小和设置
4. 手动编辑（可选）
5. 选择配色方案
6. 导出结果

### 4.3 性能优化
- Web Workers 处理大图
- 图片懒加载
- 缓存常用配色
- 增量渲染

## 五、扩展功能

### 5.1 高级功能
- 3D 预览模式
- 动画效果生成
- 社区分享平台
- 拼豆购买链接集成

### 5.2 商业化功能
- 会员订阅制
- 高级图案库
- 专属配色方案
- 品牌定制

### 5.3 AI 增强功能
- AI 辅助设计
- 风格迁移
- 智能优化建议
- 自动修复功能

## 六、部署计划

### 6.1 开发阶段
1. MVP 版本（3个月）
   - 基本图像转换
   - 简单导出功能
   - Web 端实现

2. 增强版本（2个月）
   - 高级编辑功能
   - 移动端适配
   - 社区功能

3. 完整版本（3个月）
   - AI 功能集成
   - 3D 渲染
   - 商业化功能

### 6.2 部署策略
- **Web 版**：Cloudflare + Vercel
- **桌面版**：Electron（可选）
- **移动端**：React Native（可选）

## 七、项目规划

### 7.1 里程碑
1. 第1个月：核心算法开发
2. 第2个月：前端界面实现
3. 第3个月：基础功能测试
4. 第4个月：用户反馈收集
5. 第5个月：功能优化迭代

### 7.2 团队配置
- 前端开发：2人
- 算法工程师：1人
- UI/UX设计：1人
- 测试工程师：1人

## 八、风险评估

### 8.1 技术风险
- 颜色量化算法效果不佳
- 大图处理性能问题
- 浏览器兼容性

### 8.2 市场风险
- 竞品模仿
- 用户习惯培养
- 商业化难度

### 8.3 应对策略
- 持续优化算法
- 建立技术壁垒
- 快速迭代产品

---

*方案制定日期：2026-04-21*